apiVersion: v1
kind: Service
metadata:
  labels:
    app: decks-service
  name: decks-service
  namespace: default
  annotations: null
spec:
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
  selector:
    app: decks-service
